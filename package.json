{
  "name": "base-para-aplicativo-web",
  "version": "1.0.0",
  "private": true,
  "scripts": { 
    "docker:up": "docker compose up -d",
    "db:push": "prisma generate && prisma db push",
    "predev": "npm run docker:up && npm run db:push",
    "dev": "next dev -p 3000",
    "build": "prisma generate && next build",
    "start": "next start -p 3000",
    "prisma:generate": "prisma generate",
    "prisma:migrate": "prisma migrate dev",
    "postinstall": "prisma generate"
  },
  "dependencies": {
    "@next-auth/prisma-adapter": "^1.0.7",
    "@prisma/adapter-pg": "^7.8.0",
    "@prisma/client": "^7.8.0",
    "dotenv": "^17.4.2",
    "next": "^16.2.7",
    "next-auth": "^4.24.14",
    "nodemailer": "^7.0.7",
    "pg": "^8.21.0",
    "prisma": "^7.8.0",
    "react": "^19.2.7",
    "react-dom": "^19.2.7",
    "react-imask": "^7.6.1"
  }
}
